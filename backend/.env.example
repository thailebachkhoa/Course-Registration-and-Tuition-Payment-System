# --- Server Configuration ---
PORT=5000
NODE_ENV=development

# --- Database Configuration (MySQL) ---
DB_HOST=localhost
DB_USER=root
DB_PASS=your_password_here
DB_NAME=course_registration
DB_PORT=3306

# --- Authentication (JWT) ---
# Bạn có thể tạo một chuỗi ngẫu nhiên bất kỳ cho JWT_SECRET
JWT_SECRET=your_super_secret_key_123
JWT_EXPIRES_IN=7d

# --- System Constants ---
TUITION_PER_CREDIT=600000